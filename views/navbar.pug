.container
  .row
    .twelve.columns
      img(src='../img/logo.png' alt='143chars Logo')
.container
  .row
    .twelve.columns
      a.button(href='/') Home
      if !currentUser
        a.button(href='/register') Register
      if currentUser
        a.button(href='/profile') Profile
        a.button(href='/new') New Msg
        a.button(href='/logout') Log Out
      else
        a.button(href='/login') Log In